# Runtime dependencies for `main.py --mode api`.
# Camera-only code paths (ATTENDANCE_CAMERA_INDEX, /attendance/live/*) are not
# needed on Render — the frontend sends captured frames as base64 over HTTP.

fastapi==0.115.6
uvicorn==0.32.1
python-dotenv==1.0.1

# MongoDB (async driver + fallback used in main.py)
motor==3.6.0
pymongo==4.9.2

# Vision / face recognition
numpy==1.26.4
opencv-python-headless==4.10.0.84
# dlib compiles from source and needs a working C++ toolchain + cmake;
# it can take several minutes to build and needs >1GB RAM during compile,
# which can OOM on Render's free tier. If the build fails or times out,
# swap this line for `dlib-bin==19.24.6` (prebuilt wheel, no compilation).
cmake==3.31.2
dlib==19.24.6
face_recognition==1.3.0
face_recognition_models==0.3.0
